# Service Worker Version 1.0 

## Files

- custom-sw.js: New service worker
- service-worker.js: updated default service worker of CRA

## Instructions

Place the custom service worker in the public folder and replace the old service-worker.js file with the new one.
Build / Deploy project and the custom service worker should run instead of the default one.

